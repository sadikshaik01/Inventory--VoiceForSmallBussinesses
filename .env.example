# This root file is a reference. Runtime files belong in frontend/ and backend/.
# Copy frontend/.env.example to frontend/.env for frontend configuration.
# Copy backend/.env.example to backend/.env for backend configuration.
# Never put database passwords, JWT secrets, or AI keys in a VITE_ variable.
